name: Reusable workflow
on: workflow_call # para decirle que va hacer llamado desde otro workflow
jobs:
  reusabe:
    runs-on: ubuntu-latest
    steps:
      - name: output information
        run: echo "Deploying & uploading ..."
